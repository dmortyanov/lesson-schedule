# Инструкция по восстановлению настроек PostgreSQL

## 📋 Что было сделано

Ваши оригинальные настройки PostgreSQL были временно заменены на SQLite для работы с WAMP Server.

## 🔄 Как вернуть настройки PostgreSQL

### Способ 1: Автоматический (рекомендуется)

Запустите скрипт восстановления:
```powershell
python RESTORE_POSTGRES.py
```

### Способ 2: Вручную

Откройте файл `schedule/settings.py` и найдите блок `DATABASES` (строки 67-72).

Замените:
```python
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.sqlite3",
        "NAME": BASE_DIR / "db.sqlite3",
    }
}
```

На:
```python
DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "NAME": os.getenv("POSTGRES_DB", "schedule_db"),
        "USER": os.getenv("POSTGRES_USER", "postgres"),
        "PASSWORD": os.getenv("POSTGRES_PASSWORD", "postgres"),
        "HOST": os.getenv("POSTGRES_HOST", "localhost"),
        "PORT": int(os.getenv("POSTGRES_PORT", "5432")),
    }
}
```

Также удалите комментарии о временной конфигурации (строки 63-65).

## 📝 Оригинальные настройки

Полный код для восстановления сохранён в файле: `POSTGRES_SETTINGS_BACKUP.txt`

## ⚠️ После восстановления

1. Убедитесь, что PostgreSQL запущен
2. Запустите миграции:
   ```powershell
   python manage.py migrate
   ```

---

**Примечание:** После восстановления настроек PostgreSQL, файл `db.sqlite3` можно удалить (если он был создан).

