# Настройка MySQL для работы с WAMP Server

## 📋 Что нужно сделать

Ваш проект использует `JSONField`, который требует MySQL (или PostgreSQL). SQLite не поддерживает JSONField.

## 🔧 Шаги настройки

### 1. Создайте базу данных в MySQL

Через phpMyAdmin (WAMP Server → phpMyAdmin):
1. Откройте http://localhost/phpmyadmin
2. Нажмите "New" (Новая база данных)
3. Имя базы: `schedule_db`
4. Кодировка: `utf8mb4_unicode_ci`
5. Нажмите "Create"

Или через командную строку MySQL:
```sql
CREATE DATABASE schedule_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 2. Установите драйвер MySQL для Python

Выберите один из вариантов:

#### Вариант A: PyMySQL (проще, рекомендуемый)
```powershell
pip install PyMySQL
```

Затем скопируйте содержимое файла `schedule/__init__.pymysql.example` в `schedule/__init__.py`
Или вручную добавьте в начало файла `schedule/__init__.py`:
```python
try:
    import pymysql
    pymysql.install_as_MySQLdb()
except ImportError:
    pass
```

#### Вариант B: mysqlclient (быстрее, но требует компиляторы)
```powershell
pip install mysqlclient
```
*Примечание: может потребоваться установка Visual C++ Build Tools*

### 3. Настройте переменные окружения (опционально)

Создайте файл `.env` в корне проекта (если его нет):
```
MYSQL_DB=schedule_db
MYSQL_USER=root
MYSQL_PASSWORD=          # Оставьте пустым, если у root нет пароля
MYSQL_HOST=localhost
MYSQL_PORT=3307
```

**Примечание:** По умолчанию в настройках:
- Пользователь: `root`
- Пароль: `123` (настроено)
- Порт: `3307` (настроено)
- Хост: `localhost`

Если у вас другой пароль, создайте файл `.env` с переменной `MYSQL_PASSWORD=ваш_пароль`

### 4. Запустите миграции

```powershell
python manage.py migrate
```

### 5. Создайте суперпользователя (если нужно)

```powershell
python manage.py createsuperuser
```

### 6. Запустите сервер

```powershell
python manage.py runserver
```

## ✅ Проверка

Если всё настроено правильно, вы должны увидеть:
```
Operations to perform:
  Apply all migrations: ...
Running migrations:
  ...
```

Без ошибок о JSONField!

---

## 🔄 Возврат к PostgreSQL

Когда будете готовы вернуться к PostgreSQL, см. файл `README_POSTGRES_RESTORE.md`

